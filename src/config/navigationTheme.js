import { DefaultTheme } from "@react-navigation/native";

export default NavigationTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    background: "#000",
    notification: "red",
  },
};
